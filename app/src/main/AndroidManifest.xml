<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kaseka.boxmaptest1">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.MY_PERMISSIONS_REQUEST_READ_CONTACTS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:name=".application.MyAplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity-->
            <!--android:name=".activity.ClockFaceActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->

        <activity
            android:name=".activity.AlarmActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name="com.mapbox.mapboxsdk.telemetry.TelemetryService" />
        <service
            android:name=".service.TripAlarmStartedService"
            android:exported="false" />
        <service
            android:name=".service.TripAlarmBoundService"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/googleToken" />


        <activity android:name=".activity.AlarmsListActivity" />
        <activity android:name=".activity.ClockFaceActivity" />
        <activity android:name=".activity.AlarmEditActivity"/>
    </application>

</manifest>